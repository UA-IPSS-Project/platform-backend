# Production Profile - Docker Configuration

# PostGres Config for Docker
# Use service name 'postgres' instead of 'localhost' when running in Docker
spring.datasource.url=jdbc:postgresql://postgres:5432/florinhas
spring.datasource.username=admin
spring.datasource.password=admin123

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
