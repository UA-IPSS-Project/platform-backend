package pt.florinhas.requisicoes.repository;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

class TransporteRepositoryTest {

    @Test
    void methods_DeveExistir() {

        assertEquals(
                "findByCodigo",
                "findByCodigo");

        assertEquals(
                "findByMatricula",
                "findByMatricula");

        assertEquals(
                "findByCategoria",
                "findByCategoria");

        assertEquals(
                "updateCategoriaByCategoria",
                "updateCategoriaByCategoria");
    }
}