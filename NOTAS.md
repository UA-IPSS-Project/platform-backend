## Instruções

### Para construir a imagem do postgres ( se necessário, podem verificar usando o comando *docker images*) 

    docker build -t postgres -f Dockerfile

### Correr o compose para criar container

    docker-compose up -d

